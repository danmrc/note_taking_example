# Exemplo

Este é um arquivo exemplo!

O bom do do MyST é que você pode fazer referência as equações. Por exemplo:

$$
\min_{\beta} \sum_{i=1}^n y_i - x_i \beta
$$(eqn:mqo)

A equação {eq}`eqn:mqo` é o problema de MQO!
